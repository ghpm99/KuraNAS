SELECT
    id,
    file_id,
    PATH,
    format_name,
    size,
    duration,
    WIDTH,
    HEIGHT,
    frame_rate,
    nb_frames,
    bit_rate,
    codec_name,
    codec_long_name,
    pix_fmt,
    LEVEL,
    profile,
    aspect_ratio,
    audio_codec,
    audio_channels,
    audio_sample_rate,
    audio_bit_rate,
    created_at
FROM
    video_metadata
WHERE
    id = $1;