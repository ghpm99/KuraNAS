name: Auto Tag Dry Run

# Preview da próxima tag na develop, sem criar nada.

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

jobs:
  preview:
    uses: ghpm99/ghpm99/.github/workflows/auto-tag.yml@v1
    with:
      version-scheme: calver4
      dry-run: true
